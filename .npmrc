# @bemoshiur packages are published to GitHub Packages.
# Auth is NOT stored here — set NODE_AUTH_TOKEN (a GitHub token with
# read:packages / write:packages) in your environment when installing/publishing,
# or add the token line to your personal ~/.npmrc. See PUBLISHING.md.
@bemoshiur:registry=https://npm.pkg.github.com
